# Content Unlocker

PunchLoader 的内容解锁模组。当前版本将 14 种存在于游戏资源中、但未登记到原版
`ColorGUI` 的颜色追加到颜色仓库。

## 当前功能

- 保留原版 30 种颜色及其收集进度。
- 打开颜色仓库时追加 14 种隐藏颜色。
- 自动扩展仓库页数；不会改写原版 `colors[30]` 存档数组或全颜色成就。
- 选中的隐藏颜色会像原版颜色一样写入 Build 的 `partPalets`，因此保存配装后可恢复。

`oldtextures` 中没有材质封装的废弃纹理不属于本阶段解锁范围。
